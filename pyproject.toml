[tool.poetry]
name = "rdl-python-poetry-tool"
version = "0.1.0"
description = "Test fixture"
authors = ["Test <test@example.com>"]

[tool.poetry.dependencies]
python = "^3.11"
requests = "^2.31.0"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
